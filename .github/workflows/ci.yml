name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: pytest (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install project (editable) with dev extras
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[tui]"
          pip install pytest pytest-cov pytest-asyncio

      - name: Run pytest with coverage (fail-under from pyproject)
        # ``--cov-config`` を明示しないと pytest-cov 7.x が
        # pyproject の ``[tool.coverage.report]`` を読まないケースがある
        # (``pragma: no cover`` / ``exclude_lines`` が効かず stmt 数がズレる)．
        # ``fail_under`` も pyproject 側に一元化したので CLI 側では指定しない．
        run: |
          pytest \
            --cov=anno_save_analyzer \
            --cov-config=pyproject.toml \
            --cov-report=xml \
            --cov-report=term-missing \
            --cov-branch

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unittests
          fail_ci_if_error: false

  lint:
    name: ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install ruff
        run: pip install "ruff>=0.5"
      - name: ruff check
        run: ruff check src tests
      - name: ruff format check
        run: ruff format --check src tests
